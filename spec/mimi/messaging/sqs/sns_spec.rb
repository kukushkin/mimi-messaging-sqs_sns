RSpec.describe Mimi::Messaging::Sqs::Sns do
  it "has a version number" do
    expect(Mimi::Messaging::Sqs::Sns::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
